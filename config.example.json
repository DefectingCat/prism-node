{
  // 服务器监听地址和端口
  "addr": "127.0.0.1:10808",

  // SOCKS 代理服务器地址和端口
  "socks_addr": "127.0.0.1:13659",

  // 日志文件路径（可选，默认为空）
  "log_path": "./logs/prism-node.log",

  // 域名白名单配置（可选）
  // 匹配到白名单中的域名时，将直接连接目标服务器，不通过 SOCKS 代理
  // 支持精确匹配和通配符匹配（如 *.example.com 匹配 sub.example.com）
  "whitelist": ["example.com", "*.google.com", "localhost"]
}
